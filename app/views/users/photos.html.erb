<h3><%= "#{@user.name}'s Photos" %></h3>


<div id='photos-container'>
  <% @posts.each do |post| %>
    <a href=<%= post_path(post)%>>
      <%= image_tag url_for(post.photo) %>
    </a>
  <% end %>
</div>