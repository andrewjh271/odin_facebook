Social Scrolls

Someone has attempted to update Odin's account. But don't worry — they failed.
Attempted update occurred at <%=Time.use_zone("America/Los_Angeles") { Time.current }.strftime("%m/%d/%Y, %H:%M")%>.

User IP Address: <%= @ip %>
<% unless @address.blank? %>
User Location: <%= @address %>
<% end %>